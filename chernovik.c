#include <stdio.h>
#include <stdint.h>
#include "ft_printf.h"

int main(void)
{
	d.d = 0.5078125;
	int *a = fraction(0, -(dexp()));
	printf("%i\n\n", dexp());
	while (*a != -1)
	{
		printf("%i\n", *a);
		a++;
	}
	return(0);
}