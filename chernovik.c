#include <stdio.h>
#include <stdint.h>
#include "ft_printf.h"

int main(void)
{
	ld.ld = 9.6;
	printf("%s", ldoubletostr(0));
	/*for (int i = 0; i < a->size; i++)
	{
		printf("%s", a->value[i]);
	}*/
	return(0);
}